export function DisclaimerBanner() {
  return (
    <p className="disclaimer-banner" role="note">
      Movement observations only — not medical advice. Scores reflect what the
      camera could see in this set and may vary with setup and lighting.
    </p>
  )
}
