# P4-M14 availability decision

Status: fail-closed disposition recorded; G-AVAIL blocked.

ADR-016 records `remain unavailable`, enumerates blocked gates, residual risks, revalidation/rollback triggers, and the only next authorized action. The decision does not change protocol source, status, input modes, profile, runtime, UI, deployment, or release state.

Automated tests pin the binding document and current registry/runtime fail-closed behavior. Any alternative requires the full evidence packet and all domain countersignatures in a new superseding decision.
