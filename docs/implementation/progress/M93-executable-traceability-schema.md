# M93 — Executable research-to-product traceability schema

**Status:** complete

Added deterministic versioned records connecting source/evidence status,
concept, landmarks/timing signals, active metric, threshold interpretation,
coaching rule, copy strength, validation requirement, and failure modes. Rejected
single-RGB knee-load trace remains explicit. Runtime behavior is unchanged.
