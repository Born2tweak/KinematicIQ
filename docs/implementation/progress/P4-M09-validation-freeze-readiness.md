# P4-M09 validation-freeze readiness

Status: autonomous lint/calculation/certificate tooling complete; no validation package is frozen.

The implementation requires all named count/event gates and their endpoint, unit, population/setup, threshold, CI, denominator, missingness, owner, evidence, and consequence fields. It detects allocation leakage, calculates zero-event/proportion/mean precision sizes from explicit assumptions, requires five signed roles and human authority, and hashes a canonically ordered package only when every gate closes.

Repository tests use an explicitly synthetic signature fixture to verify code paths; it is not an approval or freeze certificate. M08 has no selected real-data candidate, no locked subjects/manifests were supplied, and the statistics/custodian/validation/privacy/biomechanics owners have not signed. `G-FREEZE` remains blocked.
