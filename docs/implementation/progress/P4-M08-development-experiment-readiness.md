# P4-M08 development experiment readiness

Status: autonomous deterministic experiment package complete; candidate selection blocked.

The package validates one preregistered candidate grid, requires every sequence to run every candidate, hashes configurations, resamples whole subjects with a seeded bootstrap, reports denominators/CIs/false activation/dropout/lag/worst cases, applies fixed guardrails, and deterministically selects or retains the baseline. Synthetic input is hard-blocked from selection.

No qualified real M07 labels exist, so no production threshold, filter, persistence, return, dropout, or recovery candidate was selected or integrated. `G-EXPERIMENT` remains open.
